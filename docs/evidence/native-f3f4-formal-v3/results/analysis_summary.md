# Archived Candidate Analysis - Not Accepted Evidence

The previous Markdown summary presented candidate aggregate values as a
qualified native result. That qualification is withdrawn.

The public package does not contain the raw CTF, ROS 2, RuntimeEvent,
bpftrace/eBPF, and per-event identity records needed to independently verify
the session and recompute its claims. Numeric files retained in this directory
are archival inputs only and must not be cited as completed Native Linux,
F3/F4, scheduler, syscall, diagnosis, optimization, ECU/HIL, or hardware
results.

Repopulate this summary only after a new clean native session passes the
current environment, identity, completeness, manifest, and checksum contracts.
